extends base.pug

block site_body
    section.home-cover
        .home-info
            a.home-logo(href!=url_for())
                img(src!=url_for(theme.logo))
                h1= theme.title
            p.home-sologn #{theme.sologn}
            ul.home-menu
                each url, index in theme.home_menu
                    li
                        a(href=url)= index




